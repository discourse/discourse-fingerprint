en:
  site_settings:
    fingerprint_enabled: "Enable device fingerprinting and matching per user to identify duplicate users."
    fingerprint_cookie: "Use cookies for fingerprinting user browsers."
    fingerprint_ip: "Use IPs to create fingerprints."
    max_fingerprints: "Maximum number of fingerprints stored for each user."
  fingerprint:
    silenced: 'silenced automatically per fingerprint'
